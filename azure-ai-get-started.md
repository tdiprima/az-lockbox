# Get started with Azure AI

Jump right in with the essentials you need to build and deploy a chat app that uses models and prompts.

To find the tutorials, go to Azure AI Foundry > Code

---

❌ Hardcoding secrets, even in private repos, is a security risk.<br>
✅ Use `.env` + `.gitignore`, GitHub Secrets, or Azure Key Vault instead.

💡 *Because if something can go wrong, it eventually will!* 😅
